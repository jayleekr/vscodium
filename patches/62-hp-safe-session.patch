# HypeProof #306 — persist:hp-safe hardened integrated-browser session for minor cohorts.
# Adds a deny-all Electron session (permissions/downloads/popups/devtools) with a URL
# navigation allowlist, selected per-cohort via the hypeproof.browser.safeSession setting.
# Default off → upstream integrated browser is byte-for-byte unchanged.
diff --git a/src/vs/platform/browserView/common/browserView.ts b/src/vs/platform/browserView/common/browserView.ts
index ca9ad6d..ca044ac 100644
--- a/src/vs/platform/browserView/common/browserView.ts
+++ b/src/vs/platform/browserView/common/browserView.ts
@@ -176,6 +176,20 @@ export enum BrowserViewStorageScope {
 	Ephemeral = 'ephemeral'
 }
 
+/**
+ * HypeProof #306 — options for the hardened "safe" browser session used by minor
+ * cohorts. When passed to {@link IBrowserViewService.getOrCreateBrowserView} the
+ * view is backed by the `persist:hp-safe` session instead of the normal scope.
+ */
+export interface IBrowserSafeSessionOptions {
+	/**
+	 * URL allowlist. `file://`, `about:` and localhost are always permitted;
+	 * every other navigation must match an entry (URL prefix or host suffix).
+	 * An empty list therefore confines the view to local pages only.
+	 */
+	readonly allowlist: readonly string[];
+}
+
 export const ipcBrowserViewChannelName = 'browserView';
 
 /**
@@ -217,8 +231,10 @@ export interface IBrowserViewService {
 	 * @param id The browser view identifier
 	 * @param scope The storage scope for the browser view. Ignored if the view already exists.
 	 * @param workspaceId Workspace identifier for session isolation. Only used if scope is 'workspace'.
+	 * @param safe HypeProof #306 — when provided, back the view with the hardened
+	 *             `persist:hp-safe` session (minor cohorts). Overrides `scope`.
 	 */
-	getOrCreateBrowserView(id: string, scope: BrowserViewStorageScope, workspaceId?: string): Promise<IBrowserViewState>;
+	getOrCreateBrowserView(id: string, scope: BrowserViewStorageScope, workspaceId?: string, safe?: IBrowserSafeSessionOptions): Promise<IBrowserViewState>;
 
 	/**
 	 * Destroy a browser view instance
diff --git a/src/vs/platform/browserView/electron-main/browserSession.ts b/src/vs/platform/browserView/electron-main/browserSession.ts
index dc23d2e..2936c4f 100644
--- a/src/vs/platform/browserView/electron-main/browserSession.ts
+++ b/src/vs/platform/browserView/electron-main/browserSession.ts
@@ -7,7 +7,7 @@ import { session } from 'electron';
 import { joinPath } from '../../../base/common/resources.js';
 import { URI } from '../../../base/common/uri.js';
 import { IApplicationStorageMainService } from '../../storage/electron-main/storageMainService.js';
-import { BrowserViewStorageScope } from '../common/browserView.js';
+import { BrowserViewStorageScope, IBrowserSafeSessionOptions } from '../common/browserView.js';
 import { BrowserSessionTrust, IBrowserSessionTrust } from './browserSessionTrust.js';
 import { FileAccess } from '../../../base/common/network.js';
 
@@ -119,6 +119,26 @@ export class BrowserSession {
 			?? new BrowserSession('global', electronSession, BrowserViewStorageScope.Global);
 	}
 
+	/**
+	 * HypeProof #306 — get or create the singleton hardened "safe" session used
+	 * by minor cohorts. Backed by a dedicated `persist:hp-safe` partition so its
+	 * cookies/storage never mix with the normal browsing session. The lockdown
+	 * (deny-all permissions, blocked downloads/popups, no devtools, navigation
+	 * allowlist) is applied in {@link configure} and in the WebContents wiring.
+	 *
+	 * The allowlist is refreshed on every call so a later open can widen/narrow
+	 * it without recreating the (shared) Electron session.
+	 */
+	static getOrCreateSafe(allowlist: readonly string[] = []): BrowserSession {
+		const electronSession = session.fromPartition('persist:hp-safe');
+		const existing = BrowserSession._bySession.get(electronSession);
+		if (existing) {
+			existing.setSafeAllowlist(allowlist);
+			return existing;
+		}
+		return new BrowserSession('hp-safe', electronSession, BrowserViewStorageScope.Global, 'safe', allowlist);
+	}
+
 	/**
 	 * Get or create a workspace-scope session for the given workspace.
 	 */
@@ -162,7 +182,13 @@ export class BrowserSession {
 		scope: BrowserViewStorageScope,
 		workspaceStorageHome: URI,
 		workspaceId?: string,
+		safe?: IBrowserSafeSessionOptions,
 	): BrowserSession {
+		// HypeProof #306 — a safe request wins over scope: the hardened session is
+		// its own partition, independent of Global/Workspace/Ephemeral storage.
+		if (safe) {
+			return BrowserSession.getOrCreateSafe(safe.allowlist);
+		}
 		switch (scope) {
 			case BrowserViewStorageScope.Global:
 				return BrowserSession.getOrCreateGlobal();
@@ -183,6 +209,9 @@ export class BrowserSession {
 
 	private readonly _trust: BrowserSessionTrust;
 
+	/** HypeProof #306 — navigation allowlist, mutable for shared safe sessions. */
+	private _allowlist: readonly string[];
+
 	private constructor(
 		/**
 		 * Unique identifier for this session.  Derived from what makes the
@@ -194,7 +223,15 @@ export class BrowserSession {
 		readonly electronSession: Electron.Session,
 		/** Resolved storage scope. */
 		readonly storageScope: BrowserViewStorageScope,
+		/**
+		 * HypeProof #306 — `'safe'` applies the hardened minor-cohort policy in
+		 * {@link configure}. Defaults to `'browse'` so every existing caller is
+		 * byte-for-byte unchanged.
+		 */
+		readonly sessionKind: 'safe' | 'browse' = 'browse',
+		allowlist: readonly string[] = [],
 	) {
+		this._allowlist = allowlist;
 		this._trust = new BrowserSessionTrust(this);
 		this.configure();
 		BrowserSession.knownSessions.add(electronSession);
@@ -222,18 +259,68 @@ export class BrowserSession {
 	 * Apply the permission policy and preload scripts to the session.
 	 */
 	private configure(): void {
-		this.electronSession.setPermissionRequestHandler((_webContents, permission, callback) => {
-			return callback(allowedPermissions.has(permission));
-		});
-		this.electronSession.setPermissionCheckHandler((_webContents, permission, _origin) => {
-			return allowedPermissions.has(permission);
-		});
+		if (this.sessionKind === 'safe') {
+			// HypeProof #306 — deny every permission (camera/mic/geolocation/
+			// notifications/clipboard/…) and cancel any download. Popup, devtools
+			// and navigation-allowlist enforcement live on the WebContents (see
+			// BrowserView) because they are per-contents, not per-session, hooks.
+			this.electronSession.setPermissionRequestHandler((_webContents, _permission, callback) => callback(false));
+			this.electronSession.setPermissionCheckHandler(() => false);
+			this.electronSession.on('will-download', (event) => event.preventDefault());
+		} else {
+			this.electronSession.setPermissionRequestHandler((_webContents, permission, callback) => {
+				return callback(allowedPermissions.has(permission));
+			});
+			this.electronSession.setPermissionCheckHandler((_webContents, permission, _origin) => {
+				return allowedPermissions.has(permission);
+			});
+		}
 		this.electronSession.registerPreloadScript({
 			type: 'frame',
 			filePath: FileAccess.asFileUri('vs/platform/browserView/electron-browser/preload-browserView.js').fsPath
 		});
 	}
 
+	/** HypeProof #306 — replace the navigation allowlist for a shared safe session. */
+	setSafeAllowlist(allowlist: readonly string[]): void {
+		this._allowlist = allowlist;
+	}
+
+	/**
+	 * HypeProof #306 — whether `url` may be navigated to in this session.
+	 *
+	 * Non-safe sessions never restrict (returns `true`). Safe sessions always
+	 * permit `file://`, `about:` and localhost (the participant's own local
+	 * pages / dev server); any other URL must match an allowlist entry, either
+	 * as a URL prefix or by host (exact host or `*.entry` suffix).
+	 */
+	isUrlAllowed(url: string): boolean {
+		if (this.sessionKind !== 'safe') {
+			return true;
+		}
+		let parsed: URL;
+		try {
+			parsed = new URL(url);
+		} catch {
+			return false;
+		}
+		if (parsed.protocol === 'file:' || parsed.protocol === 'about:') {
+			return true;
+		}
+		if (parsed.hostname === 'localhost' || parsed.hostname === '127.0.0.1') {
+			return true;
+		}
+		return this._allowlist.some(entry => {
+			if (!entry) {
+				return false;
+			}
+			if (url.startsWith(entry)) {
+				return true;
+			}
+			return parsed.hostname === entry || parsed.hostname.endsWith(`.${entry}`);
+		});
+	}
+
 	/**
 	 * Clear all session data including trust state and all browsing data.
 	 */
diff --git a/src/vs/platform/browserView/electron-main/browserView.ts b/src/vs/platform/browserView/electron-main/browserView.ts
index 23d840d..772fb0b 100644
--- a/src/vs/platform/browserView/electron-main/browserView.ts
+++ b/src/vs/platform/browserView/electron-main/browserView.ts
@@ -111,6 +111,11 @@ export class BrowserView extends Disposable implements ICDPTarget {
 		this._view.setBackgroundColor('#FFFFFF');
 
 		this._view.webContents.setWindowOpenHandler((details) => {
+			// HypeProof #306 — minor cohorts never spawn popups/new windows.
+			if (this.session.sessionKind === 'safe') {
+				return { action: 'deny' };
+			}
+
 			const location = (() => {
 				switch (details.disposition) {
 					case 'background-tab': return BrowserNewPageLocation.Background;
@@ -156,6 +161,21 @@ export class BrowserView extends Disposable implements ICDPTarget {
 			this.dispose();
 		});
 
+		// HypeProof #306 — hardened minor-cohort session: confine navigation to the
+		// allowlist and keep devtools shut. `will-navigate`/`will-redirect` cover
+		// link clicks and script-driven navigations; the initial programmatic
+		// `loadURL` is the caller's responsibility (must be local or allowlisted).
+		if (this.session.sessionKind === 'safe') {
+			const guardNavigation = (event: Electron.Event, url: string) => {
+				if (!this.session.isUrlAllowed(url)) {
+					event.preventDefault();
+				}
+			};
+			this._view.webContents.on('will-navigate', guardNavigation);
+			this._view.webContents.on('will-redirect', guardNavigation);
+			this._view.webContents.on('devtools-opened', () => this._view.webContents.closeDevTools());
+		}
+
 		this._debugger = new BrowserViewDebugger(this, this.logService);
 		this._inspector = this._register(new BrowserViewElementInspector(this));
 
@@ -437,6 +457,10 @@ export class BrowserView extends Disposable implements ICDPTarget {
 	 * Toggle developer tools for this browser view.
 	 */
 	toggleDevTools(): void {
+		// HypeProof #306 — devtools stay disabled for minor cohorts.
+		if (this.session.sessionKind === 'safe') {
+			return;
+		}
 		this._view.webContents.toggleDevTools();
 	}
 
diff --git a/src/vs/platform/browserView/electron-main/browserViewMainService.ts b/src/vs/platform/browserView/electron-main/browserViewMainService.ts
index 150bfd6..aa81bc7 100644
--- a/src/vs/platform/browserView/electron-main/browserViewMainService.ts
+++ b/src/vs/platform/browserView/electron-main/browserViewMainService.ts
@@ -7,7 +7,7 @@ import { Emitter, Event } from '../../../base/common/event.js';
 import { Disposable, DisposableMap } from '../../../base/common/lifecycle.js';
 import { VSBuffer } from '../../../base/common/buffer.js';
 import { CancellationToken, CancellationTokenSource } from '../../../base/common/cancellation.js';
-import { IBrowserViewBounds, IBrowserViewState, IBrowserViewService, BrowserViewStorageScope, IBrowserViewCaptureScreenshotOptions, IBrowserViewFindInPageOptions, BrowserViewCommandId } from '../common/browserView.js';
+import { IBrowserViewBounds, IBrowserViewState, IBrowserViewService, BrowserViewStorageScope, IBrowserViewCaptureScreenshotOptions, IBrowserViewFindInPageOptions, BrowserViewCommandId, IBrowserSafeSessionOptions } from '../common/browserView.js';
 import { IElementData } from '../../browserElements/common/browserElements.js';
 import { clipboard, Menu, MenuItem } from 'electron';
 import { IEnvironmentMainService } from '../../environment/electron-main/environmentMainService.js';
@@ -70,7 +70,7 @@ export class BrowserViewMainService extends Disposable implements IBrowserViewMa
 		super();
 	}
 
-	async getOrCreateBrowserView(id: string, scope: BrowserViewStorageScope, workspaceId?: string): Promise<IBrowserViewState> {
+	async getOrCreateBrowserView(id: string, scope: BrowserViewStorageScope, workspaceId?: string, safe?: IBrowserSafeSessionOptions): Promise<IBrowserViewState> {
 		if (this.browserViews.has(id)) {
 			// Note: scope will be ignored if the view already exists.
 			// Browser views cannot be moved between sessions after creation.
@@ -82,7 +82,8 @@ export class BrowserViewMainService extends Disposable implements IBrowserViewMa
 			id,
 			scope,
 			this.environmentMainService.workspaceStorageHome,
-			workspaceId
+			workspaceId,
+			safe
 		);
 
 		const view = this.createBrowserView(id, browserSession);
diff --git a/src/vs/workbench/contrib/browserView/common/browserView.ts b/src/vs/workbench/contrib/browserView/common/browserView.ts
index 47b003b..8724d44 100644
--- a/src/vs/workbench/contrib/browserView/common/browserView.ts
+++ b/src/vs/workbench/contrib/browserView/common/browserView.ts
@@ -344,9 +344,17 @@ export class BrowserViewModel extends Disposable implements IBrowserViewModel {
 		// Always use ephemeral sessions for untrusted workspaces
 		const dataStorage = isWorkspaceUntrusted ? BrowserViewStorageScope.Ephemeral : dataStorageSetting;
 
+		// HypeProof #306 — when the active cohort is a minor one the extension sets
+		// `hypeproof.browser.safeSession`; the view is then backed by the hardened
+		// `persist:hp-safe` session with the configured navigation allowlist. Absent
+		// / false, behavior is unchanged upstream.
+		const hpSafe = this.configurationService.getValue<boolean>('hypeproof.browser.safeSession') === true;
+		const hpAllowlist = this.configurationService.getValue<string[]>('hypeproof.browser.safeAllowlist') ?? [];
+		const safe = hpSafe ? { allowlist: hpAllowlist } : undefined;
+
 		const workspaceId = this.workspaceContextService.getWorkspace().id;
 		const state = create
-			? await this.browserViewService.getOrCreateBrowserView(this.id, dataStorage, workspaceId)
+			? await this.browserViewService.getOrCreateBrowserView(this.id, dataStorage, workspaceId, safe)
 			: await this.browserViewService.getState(this.id);
 
 		this._url = state.url;
diff --git a/src/vs/workbench/contrib/browserView/electron-browser/browserView.contribution.ts b/src/vs/workbench/contrib/browserView/electron-browser/browserView.contribution.ts
index be1e6c7..47e4651 100644
--- a/src/vs/workbench/contrib/browserView/electron-browser/browserView.contribution.ts
+++ b/src/vs/workbench/contrib/browserView/electron-browser/browserView.contribution.ts
@@ -19,6 +19,7 @@ import { Schemas } from '../../../../base/common/network.js';
 import { IBrowserViewCDPService, IBrowserViewWorkbenchService } from '../common/browserView.js';
 import { BrowserViewWorkbenchService } from './browserViewWorkbenchService.js';
 import { BrowserViewCDPService } from './browserViewCDPService.js';
+import { IConfigurationRegistry, Extensions as ConfigurationExtensions } from '../../../../platform/configuration/common/configurationRegistry.js';
 
 // Register actions and browser features
 import './browserViewActions.js';
@@ -96,3 +97,26 @@ registerWorkbenchContribution2(BrowserEditorResolverContribution.ID, BrowserEdit
 
 registerSingleton(IBrowserViewWorkbenchService, BrowserViewWorkbenchService, InstantiationType.Delayed);
 registerSingleton(IBrowserViewCDPService, BrowserViewCDPService, InstantiationType.Delayed);
+
+// HypeProof #306 — per-cohort hardened browser session. The chat extension sets
+// these from the active cohort profile (`browser_session: 'safe'` → minor). When
+// the flag is off/absent the integrated browser is unchanged from upstream.
+Registry.as<IConfigurationRegistry>(ConfigurationExtensions.Configuration).registerConfiguration({
+	id: 'hypeproof.browser',
+	order: 100,
+	title: localize('hypeproof.browser.title', "HypeProof Browser"),
+	type: 'object',
+	properties: {
+		'hypeproof.browser.safeSession': {
+			type: 'boolean',
+			default: false,
+			description: localize('hypeproof.browser.safeSession', "When enabled, integrated browser tabs use a hardened session for minor cohorts: all permissions denied, downloads and popups blocked, developer tools disabled, and navigation restricted to the allowlist.")
+		},
+		'hypeproof.browser.safeAllowlist': {
+			type: 'array',
+			items: { type: 'string' },
+			default: [],
+			description: localize('hypeproof.browser.safeAllowlist', "URL allowlist for the hardened browser session. Local pages (file://, localhost) are always permitted; every other navigation must match an entry, either as a URL prefix or a host (exact or subdomain).")
+		}
+	}
+});
